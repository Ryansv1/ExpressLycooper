<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <%- include('../partials/head.ejs'); %>
    <link href="css/style-consulta.css" rel="stylesheet">
    <link href="css/hover.css" rel="stylesheet" media="all">
    <link href="css/reset.css" rel="stylesheet" media="all">
    <link href="css/navbar.css"rel="stylesheet" media="all">
    <link href="css/footer.css" rel="stylesheet" medial="all">
    
    <title><%= title %></title>
</head>
<body>
    <header>
        <%- include('../partials/header.ejs')  %>
    </header>
    <main class="container-fluid vh-100 d-flex flex-row justify-content-center align-items-center py-4">
        <div class="container-sm w-50 h-50 d-flex flex-column justify-content-center align-items-center">
            <div class="err mt-5 mb-3 text-danger" style="display: none;" >Houve um erro ao inserir os dados</div>
            <div class="err-2 mt-5 mb-3 text-danger" style="display: none;" >Não foram encontrados registros nessa data</div>
            <h3 class="mt-3 text-light text-center">Realize uma consulta específica <br>por data abaixo!</h3>
            <form class="d-flex flex-column justify-content-center align-items-center w-50 h-50" method="get" action="getAllSensorData ">
                <select class="form-select success" aria-label="Default select example" class="m-2 p-2"name="sensor">
                    <option selected>Selecione o sensor</option>
                    <option value="2">Umidade do Solo</option>
                    <option value="3" >pH</option>
                    <option value="4">Temperatura da água</option>
                </select>
                <input type="date" class="date p-2 m-2" name="dataColeta">
                <button type="submit" class="btn btn-outline-dark my-2">Pesquisa</button>
            </form>
        </div>
    </main>
    <script>
        if(window.location.search == '?err=1'){
            var err = document.getElementsByClassName('err')
            err[0].style.display = 'block'
        } else if (window.location.search == '?err=2'){
            var error = document.getElementsByClassName('err-2')
            error[0].style.display = 'block'
        }
    </script>
    <footer>
        <%- include('../partials/footer.ejs')  %> 
    </footer>
</body>
</html>